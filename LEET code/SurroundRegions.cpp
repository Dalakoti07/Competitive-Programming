class Solution {
public:
    void solve(vector<vector<char>>& board) {
        int n=board.size();
        int m=n ? board[0].size():0;
        if(n*m ==0 )return;
        if(n==2 or m==2) return;

        
    }
};