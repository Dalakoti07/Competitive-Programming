class Solution {
public:
    void gameOfLife(vector<vector<int>>& board) {
        // using extra space
        vector<vector<int>> ans=vector(board.size(),vector<int>(0,board[0].size()));

        // refer solution
    }
};