class Solution {
public:
    int maxProfit(vector<int>& prices) {
        // how we will make sure greedy apporach
        // its to find that if large gaps can be filled by many small gaps
        
    }
};